# 网页使用教程

打开 **http://127.0.0.1:5173**（需同时运行后端 `8000`）。界面是英文产品壳，下面按中文说明每一块在干什么。

当前数据窗口是 **2026-05**。顶栏黄条 *Scenario test · simulated merchant panel* 表示：商户月度面板是合成情景，用来演示流程，**不是**已上线的真实预测。

---

## 全局：顶栏

| 元素 | 作用 |
|---|---|
| 海军蓝提示条 | 提醒评委/观众：这是情景测试，不要当成生产验证 |
| As of 2026-05 | 短名单使用的预测月份（测试集最新月） |
| **Pipeline** | 首页：产品逻辑与总览数字 |
| **Strategic brief** | 战略层：给 APAC 合作伙伴看走廊/品类 |
| **Execution queue** | 执行层：给 BD 看商户短名单和签约理由 |
| **Model lab** | 证据页：给评委看模型指标，不是业务工作台 |

两个业务入口对应「双轨输出」：战略简报 vs 线索推送。

---

## 1. Pipeline（首页）

**给谁看：** 开场、讲故事。

**各块功能：**

- **标题与两句话**：说明系统只训一个商户级模型，再聚合成三层，LLM 只根据模型事实写文案。
- **四个数字卡片**
  - *Universe*：商户宇宙规模（约 1056 家）
  - *High-potential*：被标成高潜力的家数（单元内动量前 20%）
  - *Precision@30*：全局短名单前 30 名里真正高潜力的比例（模型报告里的业务主指标）
  - *Lift@30*：相对随机抽 30 家，命中率提高了多少倍
- **Open strategic brief / Open execution queue**：直接进两个工作台。
- **Five-step decision tree（五步）**
  1. Join X + Y：宏观信号与商户动量标签拼接
  2. Merchant model：商户级监督模型，打未来 3 个月动量分
  3. Three-level roll-up：拆成走廊 / 品类 / 商户三层
  4. LLM translation：三层分别生成自然语言
  5. Dual-track output：战略简报 + 执行队列
- **Step 5 两张大卡片**：点金色卡进战略层，点白卡进执行层。

**建议演示：** 先念四个数字，再顺着五步指到第 5 步两个出口。

---

## 2. Strategic brief（战略简报）

**给谁看：** APAC 合作伙伴 / 决定「先打哪条走廊、哪类商户」。

**交互逻辑（先选后生成）：** 简报不会一进页就写好。先选 **Which corridor**、再选 **Which category**，然后点 **Generate briefs**。以后走廊/品类变多时仍是同一套操作；本 demo 只有 2 条走廊 × 4 个品类，是因为当前面板数据有限，不是产品只能覆盖两个市场。

**各块功能：**

- 右上角 *Live LLM available*：已配置 API key。生成时会尝试走大模型；额度不足则退回模板，数字仍来自模型。
- **Which corridor / Which category / Generate briefs**：必选两步再出文案。改选项后需再点生成（会出现黄色提示）。
- **走廊卡片**：当前面板的排名速览；点击只是填入下拉框，**不会**自动写简报。
- **Category priority 柱状图**：选中走廊后才出现。柱越高越建议优先覆盖。品类按钮同样只是改选择。
- **Corridor brief / Category brief**：点 Generate 之后才出现。
- **Regenerate**：对当前选择重写文案，不改分数。
- 底部三格：到访/汇率（到访可能滞后，会标明月份）。
- **Drill into execution queue →**：带着已生成简报时的走廊+品类跳到 BD 队列。

**建议演示：** 下拉选 China → Singapore → 选 Food → Generate briefs → 读两段 → 下钻。

---

## 3. Execution queue（执行队列）

**给谁看：** 拓展/BD，当天要联系哪几家店。

**各块功能：**

- **Top k hit rate**：当前筛选下，短名单前 k 名里有多少家标签确实是高潜力。
- **走廊 / 品类 / Search / k 滑条**：缩小宇宙、定短名单长度。
- **左表**：排名、Score、HP、Exposure。点一行或点 **Brief** 进入商户简报页（**不能在这里直接 Push**）。
- **右图**：地图选点同样进入商户简报页。

**建议演示：** k=30 → 点一家高分店 → 进入 Merchant brief。

---

## 4. Merchant brief（商户 LLM 简报页）

**给谁看：** BD 在推送线索之前，必须先「了解这家店」。

**交互（先选、再生成、再 Push）：**

1. **Which merchant**：下拉或从队列点进来。
2. **Generate merchant brief**：调用 LLM 接口。接口不通时自动用**模板**（数字仍来自模型事实）。
3. 生成后才解锁 **Push to BD**。

左侧是结构化事实（A/B/C、动量曲线），右侧才是 LLM 写的基本信息 + 签约理由 + outreach hook。

**接口（已接好，现网额度不足会走模板）：**

- `GET /api/llm/status` — 是否配置、是否 live、熔断原因
- `POST /api/merchants/{id}/brief` — 商户专用简报
- `POST /api/explain` — `{ "level": "merchant", "id": "<merchant_id>", "use_llm": true }`

返回字段：`headline`、`profile`（基本信息）、`bullets`、`hook`、`source`（`llm` | `template`）、`llm_status`。

---

## 5. Model lab（模型实验室）

**给谁看：** 评委、问「指标怎么来的」的人。**不要**当成 BD 日常页面。

**各块功能：**

- 灰色警告段落：再次标明合成数据 / pipeline test
- **时间胶囊**
  - 深蓝 *train*：训练月
  - 金色 *embargo*：禁运期（训练和测试之间丢掉的月份，防止标签窗口重叠泄漏）
  - 描边 *test*：测试月（2026-04、2026-05）
- **指标表**
  - *gbm*：当前展示用的梯度提升模型
  - *baseline_prior*：人人预测基础率
  - *baseline_persistence*：只用滞后评论增速（「动量自我延续」零假设）
  - 列：ROC-AUC、PR-AUC、P@30、Lift@30。业务上优先看后两列
- **Feature importance**：gain 重要性，解释模型主要看哪些特征（如滞后增速、近期评论、评分、暴露度等）

---

## LLM 文案怎么理解

- 数字只来自模型表，LLM **不许编造**到访量、评分、距离。
- `source: llm`：这次是大模型写的；`source: template`：模板（无额度、无 key 或调用失败）。
- 当前若 OpenAI 账号没额度，Regenerate 仍会出模板，界面可能提示失败原因。

---

## 推荐 2 分钟路演顺序

1. **Pipeline**：宇宙 → 短名单，念 P@30 / Lift  
2. **Strategic brief**：选走廊 → 选品类 → Generate briefs → 读两段  
3. **Drill into execution queue**  
4. k=30，点 Brief 进商户页 → Generate merchant brief → Push  
5. 有人问指标再进 **Model lab**
